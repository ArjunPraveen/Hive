# Hive — Claude Code Instructions

## Before every push

Always update the **Iterations table** in `README.md` before pushing. Add a new row with the next iteration number, today's date, and a summary of what changed. Never push without doing this.
